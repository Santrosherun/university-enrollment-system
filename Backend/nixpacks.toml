[phases.setup]
nixPkgs = [
    "python311",
    "pango",
    "cairo",
    "gdk-pixbuf",
    "libffi",
    "fontconfig",
    "freetype",
    "harfbuzz",
    "gobject-introspection",
    "dejavu_fonts"
]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = []

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
